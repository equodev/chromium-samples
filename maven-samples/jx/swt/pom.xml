<project xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<groupId>equochromium</groupId>
	<artifactId>swt</artifactId>
	<packaging>jar</packaging>
	<version>1.0-SNAPSHOT</version>

	<properties>
		<maven.compiler>11</maven.compiler>
		<swt.version>3.121.0</swt.version>
		<chromium.version>116.0.1</chromium.version>
		<chromium.platform.version>116.0.1</chromium.platform.version>
		<chromium.jx.version>116.0.0.0</chromium.jx.version>
		<gdk.backend.value></gdk.backend.value>
	</properties>

	<repositories>
		<repository>
			<id>Equo-Chromium</id>
			<url>https://dl.equo.dev/chromium-swt-ee/equoSamples/mvn</url>
		</repository>
	</repositories>

	<dependencies>
		<dependency>
			<groupId>org.eclipse.platform</groupId>
			<artifactId>org.eclipse.swt.${platform}.x86_64</artifactId>
			<version>${swt.version}</version>
		</dependency>
		<dependency>
			<groupId>com.equo</groupId>
			<artifactId>com.equo.chromium.jx</artifactId>
			<version>${chromium.jx.version}</version>
		</dependency>
		<dependency>
			<groupId>com.equo</groupId>
			<artifactId>com.equo.chromium.cef.${platform}.x86_64</artifactId>
			<version>${chromium.platform.version}</version>
		</dependency>
	</dependencies>

	<build>
		<sourceDirectory>src</sourceDirectory>
		<plugins>
			<plugin>
				<artifactId>maven-compiler-plugin</artifactId>
				<version>3.3</version>
				<configuration>
					<source>${maven.compiler}</source>
					<target>${maven.compiler}</target>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.codehaus.mojo</groupId>
				<artifactId>exec-maven-plugin</artifactId>
				<version>3.1.0</version>
				<executions>
					<execution>
						<phase>verify</phase>
						<goals>
							<goal>exec</goal>
						</goals>
					</execution>
				</executions>
				<configuration>
					<executable>java</executable>
					<arguments>
						<argument>${flags}</argument>
						<argument>-classpath</argument>
						<classpath />
						<argument>equochromium.SampleSWT</argument>
					</arguments>
					<environmentVariables>
						<GDK_BACKEND>${gdk.backend.value}</GDK_BACKEND>
					</environmentVariables>
				</configuration>
			</plugin>
		</plugins>
	</build>

	<profiles>
		<profile>
			<id>linux</id>
			<activation>
				<os>
					<family>linux</family>
				</os>
			</activation>
			<properties>
				<gdk.backend.value>x11</gdk.backend.value>
				<flags>-Dchromium.init_threads=true</flags>
				<platform>gtk.linux</platform>
			</properties>
		</profile>
		<profile>
			<id>windows</id>
			<activation>
				<os>
					<family>windows</family>
				</os>
			</activation>
			<properties>
				<flags>-Dempty</flags>
				<platform>win32.win32</platform>
			</properties>
		</profile>
		<profile>
			<id>mac</id>
			<activation>
				<os>
					<family>mac</family>
				</os>
			</activation>
			<properties>
				<flags>-XstartOnFirstThread</flags>
				<platform>cocoa.macosx</platform>
			</properties>
		</profile>
	</profiles>
</project>
