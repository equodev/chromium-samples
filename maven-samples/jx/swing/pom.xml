<project xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<groupId>equochromium</groupId>
	<artifactId>swing</artifactId>
	<packaging>jar</packaging>
	<version>1.0-SNAPSHOT</version>

	<properties>
		<maven.compiler>11</maven.compiler>
		<flags>-Dempty</flags>
		<chromium.version>116.0.1</chromium.version>
		<chromium.platform.version>116.0.1</chromium.platform.version>
		<chromium.jx.version>116.0.0.0</chromium.jx.version>
	</properties>

	<repositories>
		<repository>
			<id>Equo-Chromium</id>
			<url>https://dl.equo.dev/chromium-swt-ee/equoSamples/mvn</url>
		</repository>
	</repositories>

	<dependencies>
		<dependency>
			<groupId>com.equo</groupId>
			<artifactId>com.equo.chromium.jx</artifactId>
			<version>${chromium.jx.version}</version>
		</dependency>
		<dependency>
			<groupId>com.equo</groupId>
			<artifactId>com.equo.chromium.cef.${platform}.x86_64</artifactId>
			<version>${chromium.platform.version}</version>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<artifactId>maven-compiler-plugin</artifactId>
				<version>3.3</version>
				<configuration>
					<source>${maven.compiler}</source>
					<target>${maven.compiler}</target>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.codehaus.mojo</groupId>
				<artifactId>exec-maven-plugin</artifactId>
				<version>3.1.0</version>
				<executions>
					<execution>
						<phase>verify</phase>
						<goals>
							<goal>exec</goal>
						</goals>
					</execution>
				</executions>
				<configuration>
					<executable>java</executable>
					<commandlineArgs>${flags} -classpath %classpath equochromium.SampleSwing</commandlineArgs>
				</configuration>
			</plugin>
		</plugins>
	</build>

	<profiles>
		<profile>
			<id>linux</id>
			<activation>
				<os>
					<family>linux</family>
				</os>
			</activation>
			<properties>
				<platform>gtk.linux</platform>
			</properties>
		</profile>
		<profile>
			<id>windows</id>
			<activation>
				<os>
					<family>windows</family>
				</os>
			</activation>
			<properties>
				<platform>win32.win32</platform>
			</properties>
		</profile>
		<profile>
			<id>mac</id>
			<activation>
				<os>
					<family>mac</family>
				</os>
			</activation>
			<properties>
				<platform>cocoa.macosx</platform>
				<flags>--add-opens java.desktop/java.awt=ALL-UNNAMED --add-opens java.desktop/sun.awt=ALL-UNNAMED --add-opens java.desktop/sun.lwawt=ALL-UNNAMED --add-opens java.desktop/sun.lwawt.macosx=ALL-UNNAMED</flags>
			</properties>
		</profile>
	</profiles>
</project>
